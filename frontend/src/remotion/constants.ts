/**
 * Shared render/preview constants for the Remotion motion-graphics module.
 * Imported by BOTH the live <Player> preview and the <Composition> registered
 * for rendering, so the two can never drift on dimensions, frame rate, or
 * duration.
 */
export const FPS = 30;
export const WIDTH = 1920;
export const HEIGHT = 1080;
export const DURATION_IN_FRAMES = 90;
