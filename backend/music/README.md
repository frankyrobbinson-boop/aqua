# Background music

Drop `.mp3` / `.wav` / `.m4a` files here. They play in FILENAME order (name them
`01_`, `02_`… to set the sequence) at low volume under the narration, looping to
fill the video.

The audio files themselves are git-ignored (see the root `.gitignore`); only this
README is tracked.
